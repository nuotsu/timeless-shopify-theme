<script src="https://cdn.tailwindcss.com"></script>

<script>
tailwind.config = {
  theme: {
    extend: {
      colors: {
        // base
        paint: '{{ settings.paint }}',
        ink: '{{ settings.ink }}',
        // accents
        primary: '{{ settings.primary }}',
        secondary: '{{ settings.secondary }}',
      }
    }
  }
}
</script>

<style type="text/tailwindcss">
@layer base {
  body {
    @apply bg-paint text-ink accent-primary;
  }

  summary {
    cursor: pointer;
  }
}

@layer utilities {
  .rte > * + * {
    margin-top: var(--spacing, .5em);
  }

  .rte :is(ul, ol) {
    list-style: revert;
    padding-left: 1.5em;
  }

  .rte :is(th, td) {
    padding: 0.25em 1em;
    @apply border;
  }
}
</style>
