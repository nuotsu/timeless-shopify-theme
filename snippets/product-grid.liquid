{% assign sale_badge = section.blocks | where: 'type', 'sale-badge' | first %}

<ul class="item-grid" style="--size: {{ section.settings.grid_item_size }}px;">
  {%- render 'product-item' for products as product
    sale_badge: sale_badge.settings
  -%}
</ul>
