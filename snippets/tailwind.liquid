<script src="https://cdn.tailwindcss.com"></script>

<script>
tailwind.config = {
  theme: {
    extend: {
      colors: {
        // base
        paint: '{{ settings.paint }}',
        ink: '{{ settings.ink }}',
        // accents
        primary: '{{ settings.primary }}',
        secondary: '{{ settings.secondary }}',
      },
      fontFamily: {
        heading: `{{ settings.font_heading.family }},{{ settings.font_heading.fallback_families }}`,
        body: `{{ settings.font_body.family }},{{ settings.font_body.fallback_families }}`,
      },
    }
  }
}
</script>

<style type="text/tailwindcss">
{{ settings.font_heading | font_face: font_display: string }}
{{ settings.font_body | font_face: font_display: string }}

@layer base {
  body {
    @apply bg-paint text-ink accent-primary font-body;
  }

  summary {
    cursor: pointer;
  }
}

@layer components {
  /* typography */
  ._h {
    @apply font-heading;
  }
  .h1 { @apply _h; }
  .h2 { @apply _h; }
  .h3 { @apply _h; }
  .h4 { @apply _h; }
  .h5 { @apply _h; }
  .h6 { @apply _h; }

  /* links, CTAs */

  .link {
    @apply underline decoration-gray-300 hover:decoration-primary;
  }
}

@layer utilities {
  .rte > * + * {
    margin-top: var(--spacing, .5em);
  }

  .rte :is(ul, ol) {
    list-style: revert;
    padding-left: 1.5em;
  }

  .rte :is(th, td) {
    padding: 0.25em 1em;
    @apply border;
  }
}
</style>
